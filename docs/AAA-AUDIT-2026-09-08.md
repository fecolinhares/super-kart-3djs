# Auditoria AAA completa — Super Kart 3D.js

**Data:** 2026-09-08 UTC  
**Escopo:** todas as pistas existentes (`Meadow Circuit`/track 1 e `Neon City`/track 2), gameplay ativo em desktop/mobile, vídeo em GPU real, validação visual e plano por fases.  
**Estado:** auditoria concluída; nenhum patch de produto aceito nesta rodada.

## 1. Evidência executada

### Build e gameplay determinístico

- `node --check src/main.js src/track/TrackBuilder.js src/track/Environment.js`: passou.
- `git diff --check`: passou.
- `SK3D_OUT_DIR=/tmp/sk3d-audit-build npm run build`: passou; 44 módulos, JS 908.02 kB minificado, 3.89 s. Warning existente: chunk principal >500 kB.
- `node scripts/ai-backwards-test.mjs 20 1`: 0 lost, 0 backwards, 0 crashes; onRoad 100% nos seeds.
- `node scripts/ai-backwards-test.mjs 20 2`: 0 lost, 0 backwards, 0 crashes; onRoad 100% nos seeds.
- `city-layout-probe.mjs meadow`: self-crossings 0, kerb-edge folds 0, raio mínimo 11.9 m, mas `start tangent` não satisfaz o gate legado `+Z`; requer confirmação da convenção de largada antes de virar bug.
- `city-layout-probe.mjs city`: self-crossings 0, kerb-edge folds 0, largada `+Z`, resultado PASS; raio reportado 1.4 m é um outlier de amostragem Catmull-Rom, enquanto spans de curva reportados ficaram em 7.5–9.0 m.

### Vídeo de gameplay no GPU runner

Capturador: `scripts/playtest-video.cjs`, Playwright no LXC105, flags ANGLE/Vulkan, `localStorage.clear()`, CDP screencast a 12 fps, 24 s por cenário. Renderer confirmado em todos: `ANGLE (AMD, Vulkan 1.4.318, AMD Radeon 780M, RADV PHOENIX)`. Os JPEGs foram convertidos em MP4 H.264 e contact sheets foram preservadas fora do repositório em `/tmp/sk3d-audit-20260908/`.

| Cenário | Viewport efetivo | Frames | Fase final | Vídeo |
|---|---:|---:|---|---|
| Meadow desktop | 1280×720 | 321 | race | `meadow-desktop/gameplay.mp4` |
| Neon desktop | 1280×720 | 255 | race | `neon-desktop/gameplay.mp4` |
| Meadow mobile | screencast 333×720 (contexto 390×844 touch) | 393 | finished | `meadow-mobile/gameplay.mp4` |
| Neon mobile | screencast 333×720 (contexto 390×844 touch) | 402 | race | `neon-mobile/gameplay.mp4` |
| **Total** | — | **1.371** | — | — |

Todos os vídeos foram inspecionados por contact sheet com o mesmo protocolo visual. A análise Sol/xhigh foi solicitada com o conjunto de evidências e deve ser anexada nesta seção quando o processo terminar; nenhuma nota de Sol é inventada aqui.

### Performance temporal GPU

Probe `scripts/audit-frame-time.cjs`, 5 s por cenário, WebGL2, `pageErrors=[]`, fase `race`, 1 render call/frame:

| Cenário | Samples | FPS aprox. | Frame p95 | Render p95 | Triângulos medianos |
|---|---:|---:|---:|---:|---:|
| Meadow desktop | 432 | 86.73 | 14.3 ms | 12.4 ms | 1,089,129 |
| Meadow mobile | 540 | 108.02 | 12.0 ms | 9.9 ms | 934,215 |
| Neon desktop | 552 | 110.47 | 11.1 ms | 9.3 ms | 307,940 |
| Neon mobile | 601 | 120.11 | 10.4 ms | 8.6 ms | 303,738 |

Esses números são baseline do runner, não promessa universal de FPS em hardware do usuário.

## 2. Scorecard visual provisório baseado nos vídeos

Escala 0–10; nota provisória até a crítica Sol/xhigh ser anexada. A nota não substitui A/B pareado.

| Categoria | Meadow desktop | Meadow mobile | Neon desktop | Neon mobile |
|---|---:|---:|---:|---:|
| Traçado/pista | 8.0 | 7.5 | 8.0 | 7.5 |
| Kart/câmera | 7.5 | 7.0 | 7.0 | 6.5 |
| Ambiente/densidade | 7.5 | 7.0 | 7.0 | 6.5 |
| Materiais/lighting | 7.5 | 7.0 | 6.5 | 6.0 |
| VFX/readability | 7.5 | 7.0 | 7.0 | 6.5 |
| HUD/touch | 7.5 | 7.0 | 7.5 | 7.0 |
| Estabilidade/performance | 8.5 | 8.5 | 8.5 | 8.5 |
| **Média provisória** | **7.7** | **7.3** | **7.4** | **6.9** |

Achados visuais concretos:

- Meadow: leitura de pista e cores são fortes; pórtico `FINISH` compete com a aproximação e a reta pode ficar visualmente aberta/vazia.
- Meadow mobile: controles touch e velocímetro são utilizáveis; ranking/labels e pórtico competem no portrait; o screencast efetivo de 333 px precisa ser separado do viewport CSS 390×844 na próxima captura.
- Neon: identidade cromática funciona, mas o skyline ainda lê como parede repetitiva/flat; magenta e preto dominam, reduzindo separação material e grounding em trechos escuros.
- Neon mobile: cidade e pista continuam legíveis, mas luminância comprimida, HUD nas bordas e composição do pórtico/ranking reduzem foco no kart.

## 3. Gaps priorizados

1. **Neon: grounding/material do skyline** — hipótese: fachadas emissivas/`MeshBasicMaterial` e pouca camada de contato. Teste: A/B fixo com AO/material híbrido emissive-safe; aceitar apenas melhora cega direcional em ambos viewports.
2. **Neon: repetição por zonas** — hipótese: kit procedural sem landmarks suficientes. Teste: censo de props por segmento + câmera real; aceite = 3–4 zonas visualmente reconhecíveis sem aumentar calls fora do budget.
3. **Composição mobile** — hipótese: pórtico, ranking, speedo e controles disputam o mesmo campo vertical. Teste: matriz portrait com safe-area e overlays temporariamente nomeados; aceite = kart/pista continuam dominantes durante aproximação.
4. **Meadow: retas vazias** — hipótese: densidade de props/crowd não varia com distância e zona. Teste: playtest-sim + contagem por segmento; aceite = mais ritmo sem poluir a linha ideal.
5. **Largada Meadow / probe** — hipótese não provada: o gate `+Z` não corresponde à orientação intencional de Meadow. Teste: comparar `TRACK_PATH`, tangente da câmera, grid e direção dos karts; corrigir o probe, não a pista, se a convenção estiver correta.
6. **Chunk principal >500 kB** — hipótese: falta de code-splitting efetivo devido a imports mistos. Teste: bundle visualizer/manualChunks sem mudar runtime; aceite = redução mensurável no boot mobile sem regressão.
7. **Pórtico FINISH** — repetir A/B somente em câmera de gameplay real, não crop isolado; critério = landmark reconhecível e menos obstrução em quatro combinações.
8. **Validação mobile de largura** — investigar por que screencast JPEG ficou 333 px apesar de contexto 390×844; registrar canvas/client/backing/DPR e não inferir clipping apenas do vídeo.
9. **VFX neon** — limitar bloom/haze por zona e estado; aceitar somente se contraste do traçado e skyline melhorar sem lavar o asfalto.
10. **Áudio em gameplay** — fora do escopo visual desta rodada; fase própria deve medir mix, lifecycle e não clipping com arquivos WAV.

## 4. Plano por fases

### F0 — Instrumentação e baseline reproduzível
- **Objetivo:** separar viewport CSS, canvas, framebuffer, DPR, renderer, fase e page errors.
- **Arquivos prováveis:** `scripts/playtest-video.cjs`, `scripts/audit-frame-time.cjs`, novo relatório em `qa-gpu-runner/` fora do git.
- **Evidência:** quatro vídeos, JSON de métricas, RADV PHOENIX, `pageErrors=[]`, fase `race/finished`.
- **Risco:** comparar frames em estados temporais diferentes.

### F1 — Composição e câmera
- **Objetivo:** kart como sujeito; reduzir competição do FINISH/HUD em portrait.
- **Arquivos prováveis:** `src/main.js`, CSS/HUD, `TrackBuilder.js` apenas se a geometria do pórtico for confirmada.
- **Evidência:** A/B pareado nas quatro combinações + vídeos de 24 s; prompt cego idêntico.
- **Risco:** câmera bonita em captura fixa mas pior para corrida real.

### F2 — Grounding e materiais Neon
- **Objetivo:** profundidade seletiva sem escurecer janelas emissivas.
- **Arquivos prováveis:** `src/track/Environment.js`, `src/render/MaterialLibrary.js`.
- **Evidência:** A/B com owner isolado, pixel delta, crítica visual e render budget.
- **Risco:** repetir candidatos históricos rejeitados (plinth/AO/Standard).

### F3 — Zonas e densidade de mundo
- **Objetivo:** Meadow ter ritmo nas retas; Neon ter zonas/landmarks, não parede uniforme.
- **Arquivos prováveis:** `Environment.js`, builders de props, instancing.
- **Evidência:** playtest-sim com câmera real, censo geométrico, 4 vídeos e audit-geometry.
- **Risco:** aumentar calls/triângulos ou invadir borda da pista.

### F4 — HUD e touch portrait
- **Objetivo:** hierarquia: pista/kart > velocidade/posição > controles secundários.
- **Arquivos prováveis:** `src/ui/ui.css`, `src/ui/HUD.js`, `src/main.js`.
- **Evidência:** DOM bounding boxes, touch targets ≥44 px, safe-area, vídeo mobile ativo, zero clipping.
- **Risco:** reduzir opacidade/escala e perder affordance — não aceitar por estética isolada.

### F5 — VFX e áudio legíveis
- **Objetivo:** turbo, drift, itens e áudio reforçarem estado sem haze/clip.
- **Arquivos prováveis:** partículas, `PostFX`, áudio procedural.
- **Evidência:** frames A/B temporais, diff regional, OfflineAudioContext + lifecycle browser.
- **Risco:** bloom global e partículas pequenas enganarem o crítico.

### F6 — Release matrix
- **Objetivo:** build, Pages, budget e regressão final.
- **Evidência:** 4 combinações × gameplay completo, AI 20×2, build externo, scorecard Sol/fresh-eyes, sem page errors.
- **Risco:** declarar AAA com base em screenshot único.

## 5. Abordagens proibidas nesta auditoria

- Não trocar `MeshBasicMaterial` por `MeshStandardMaterial` globalmente no Neon sem A/B; histórico já mostrou janelas escurecendo.
- Não aceitar AO/plinth/fundação apenas porque o diff de pixels é alto; pixels mudados não provam ganho.
- Não comparar notas de prompts ou estados temporais diferentes.
- Não usar SwiftShader/local como prova de qualidade GPU.
- Não ajustar câmera usando offsets fixos sem reproduzir `followCam` real.
- Não corrigir a pista Meadow só porque o probe legado exige +Z; primeiro confirmar a convenção da largada.

## 6. Sequência recomendada dos próximos commits

1. `qa(audit): separar viewport, framebuffer e fase nos vídeos desktop/mobile` — somente instrumentação; validar F0.
2. `fix(composition): reduzir competição do FINISH/HUD no portrait` — somente após A/B com o commit 1.
3. `fix(neon): grounding emissive-safe por owner` — candidato pequeno, reversível, com A/B GPU e crítica Sol/fresh-eyes.

## Artefatos

- Capturas temporárias: `/tmp/sk3d-audit-20260908/`.
- Gate ledger: `GATES.md`.
- Vídeo não é commitado no repositório; a documentação registra o caminho e métricas verificadas para evitar inflar o repo.

## Execução autônoma — 2026-09-08 — F1/F4 validados, F2 sem patch aceito

- Baseline externo em `/tmp/sk3d-dist-baseline`: `44 módulos`, JS `908.02 kB` minificado, warning de chunk >500 kB; build no worktree virtiofs falha ao copiar `public/favicon.svg` com `EPERM`, portanto `SK3D_OUT_DIR=/tmp/...` é o caminho verificável.
- AI Track 1/2 ×20 após F1/F4: `0 lost`, `0 backwards`, `0 crashes`; `onRoad=100%` nos runs observados.
- Commit `648d826` / F1: `?demo` deixou de adicionar `3.6–4.2 m` de recuo e `3.4 m` de sway lateral; passou a usar a distância normal por pista, sway `0.55 m` e altura cinematográfica reduzida. Build `44 módulos`, JS `908.03 kB`; GPU vídeo 4/4 em RADV PHOENIX, `pageErrors=[]`, fases `race/race/race/race` nesta janela; revisão visual confirmou ganho forte em Neon desktop e residual de escala/ancoragem em Meadow/mobile.
- Commit `df09d0e` / F4: controles portrait corrigidos também no override `@media (max-width:480px)`, `64×64 px`, offsets com `env(safe-area-inset-*)`; sonda DOM mobile real confirmou `pointer:coarse`, root visível, quatro alvos `64×64`, pause `52×52`, speedo `112×112`, `pageErrors=[]`. Vídeo móvel pós: Meadow `394` frames/`phase=race`, Neon `402` frames/`phase=race`, RADV PHOENIX; visão confirmou menor faixa inferior, mas residual de kart/velocímetro e minimap Neon escuro.
- F2 foi auditado sem patch: skyline já tem roof caps, pilasters, haze por row e contact-AO seletivo em `Environment.js`; plinth/AO adicional e troca global para `MeshStandardMaterial` permanecem rejeitados pelo histórico A/B. Próximo owner defensável é identidade por zonas, não repetir grounding sub-perceptual.
- Limitações: contagem de frames é captura/screencast, não FPS; viewport CSS `390×844` foi registrada separadamente da largura codificada do screencast; `phase=race` não cobre estado final.

## Execução autônoma — F3/F5/F6 — 2026-09-08

- Commit `d6d0224` / F3: skyline Neon passou a usar quatro perfis de escala por setor normalizado do path, mantendo a mesma geometria instanciada, materiais, road e física. Build `44 módulos`, JS `908.12 kB`; AI Track 1/2 ×20 zero eventos; `city-layout-probe city` PASS (0 crossings/folds, start +Z).
- F3 vídeo GPU 4/4: Meadow desktop/mobile e Neon desktop/mobile em RADV PHOENIX, `pageErrors=[]`, contagens `303/401/243/401`; revisão cega encontrou ganho positivo de silhueta/zona no Neon sem regressão na pista, kart ou HUD. Mobile ainda tem contraste escuro e semântica FINISH pendentes.
- F5 áudio: `probe-audio-lifecycle.mjs` executado no LXC105 passou `AUDIO_LIFECYCLE=PASS checks=9 failed=0 pageErrors=0`, GPU RADV PHOENIX. Probe offline local ficou bloqueado por `web-audio-api` ausente; não é usado como aprovação.
- F6 release: build externo em `/tmp` passa, mas o worktree virtiofs continua falhando em `dist` com `EPERM` ao copiar `public/favicon.svg`; warning de chunk >500 kB permanece. Matriz visual, AI e browser checks seguem verdes nos cenários executados; nenhum claim de FPS universal.

## Execução autônoma — minimap crítico — 2026-09-08

- Commit `52e77b7`: reforçado contraste do minimap (`background 0.72`, ring `0.65`, glow `0.72`) e impedido o auto-hide de reduzir navegação crítica para `opacity:0.22`; idle mobile mantém `0.88`.
- Build externo: 44 módulos, JS `908.12 kB`, warning chunk >500 kB mantido; AI Track 1/2 ×20 sem lost/backwards/crashes; sintaxe/diff hygiene verdes.
- GPU pós-patch: Meadow desktop/mobile e Neon desktop/mobile em RADV PHOENIX, `pageErrors=[]`, frames `311/397/233/400`, fase `race` nesta janela.
- Vision cega: minimap permaneceu legível sobre céu/nuvens e prédios Neon, sem competir com pista/kart/HUD; dot continua menor que a rota, mas não desaparece. Meadow não apresentou regressão.
- Limitação: resultado final não foi coberto nesta janela; frames e vídeo não provam FPS.

## Convergência autônoma — build e semântica FINISH — 2026-09-08

- Commit `4d1d831` / build: Rollup separa `index` da aplicação (`373.9 kB`) e chunks `three-examples` (`19.0 kB`) / `three-core` (`515.4 kB`). Preview de produção em quatro combinações GPU passou RADV PHOENIX, `pageErrors=[]`, sem regressão visual; warning >500 kB permanece apenas no `three-core` pré-compilado.
- Commit `148d148` / banner: texto compartilhado mudou de `FINISH` para `START / FINISH`, sem alterar geometria/câmera/HUD/física. Build e AI 20×2 passaram; vídeos GPU 4/4 e vision cega confirmaram semântica de largada mais clara em `GO`/`LAP 0/3`, sem regressão desktop/mobile. Ressalva: banner parcialmente cortado na largada mobile, já existente na composição.
- Estado: não há patch seguro adicional identificado sem A/B dedicado; F2 continua rejeitando AO/plinth/Standard global, e code-splitting não consegue dividir internamente o `three.module.js` pré-compilado sem trocar a estratégia de dependência.

## Continuação autônoma — ciclo 2 — 2026-09-08

- Primeiro A/B do banner mobile foi rejeitado: estreitar somente a geometria ainda cortava o texto. Segundo A/B (fonte 44 px) também não deu margem suficiente.
- Commit `f8aef2a`: solução composta mobile-only, pórtico `roadW-2.6` e fonte `36 px`; desktop preserva `roadW+0.8`/`58 px`. Build/AI verdes; GPU 4/4 RADV PHOENIX, `pageErrors=[]`; vision confirmou `START / FINISH` inteiro com margem em Meadow/Neon mobile e desktop intacto.
- Commit `66ce7ab`: player-dot/cone do minimap mudaram de branco para `#ff4d6d`; vision confirmou distinção da rota clara/fundo escuro em Meadow/Neon desktop/mobile sem ruído excessivo.
- Commit `3df8b2c`: ranking final agora possui callback `onRaceOver` e atualiza o card quando todos os karts têm `totalTime`; eliminou linhas persistentes com `—`. Probe 4/4 confirmou `phase=finished`, `raceOver=true`, seis tempos numéricos e `pageErrors=[]`; restart 4/4 voltou a `phase=race` com `elapsed=0`.
- Fase final visual: Meadow/Neon desktop/mobile, 60 s, RADV PHOENIX, frames `915/1002/721/1009`; vision confirmou seis linhas, botões e safe-area. Não inferir FPS.

## Métricas de boot e bundle — ciclo 2

- Preview produção em quatro combinações: renderer RADV PHOENIX e `pageErrors=[]` em todas.
- `jsTransfer=254137 B`, `jsDecoded=909118 B`; DOMContentLoaded `243–324 ms`; player pronto `426–819 ms`; `phase=race` `2930–3354 ms`.
- Bundle split de `4d1d831` preservado: app ~`374.5 kB`, `three-examples 18.96 kB`, `three-core 515.38 kB`; warning residual é o core pré-compilado. Não foi aumentado `chunkSizeWarningLimit` e não houve split especulativo adicional.
- Probe `readPixels` em grade 10×10 retornou zero no contexto usado pelo runner; resultado inconclusivo, não tratado como blank-canvas claim. Vídeo GPU permanece a evidência visual.

## Continuação autônoma — ciclo 3

- Pausa/resume 4/4: Meadow/Neon desktop/mobile abriram o overlay, retornaram a corrida e mantiveram `pageErrors=[]`.
- Menu lifecycle 4/4: menu inicial → Start Race → pausa/Menu → Start Race retornou a `phase=race` sem erro; Race Again já tinha passado no ciclo 2.
- Touch probe: Meadow/Neon mobile com `64×64` nos quatro controles, pause `52×52`, zero overlap, evento real `active→cleared`. O teste revelou `setPointerCapture` sem guarda em steer.
- Commit `24e9c46`: `bindSteer()` agora protege `setPointerCapture()` com `try/catch`; build/AI verdes e probe touch pós-fix passou com `pageErrors=[]`.
- Áudio no dev server LXC105: `AUDIO_LIFECYCLE=PASS checks=9 failed=0 pageErrors=0`, GPU RADV PHOENIX. Preview não aceita import dinâmico `/src/audio/AudioManager.js`; isso foi classificado como limitação do probe, não do produto.
- VFX/contraste: nova varredura não encontrou patch seguro; bloom/ColorGrade/AO/Standard global permanecem rejeitados por A/B.

## Continuação autônoma — ciclo 4

- Seleção de pista real: Meadow e Neon selecionados no menu mobile; `localStorage` confirmou `track=1/2`, cards `is-selected`, zero page errors.
- Defeito funcional encontrado: o botão de pausa mobile emitia `pointerdown/touchstart/pointerup/touchend`, mas o overlay não abria pelo tap real. O listener dependia de `pointerdown` e não cobria adequadamente o caminho touch do Chromium.
- Correção: `TouchControls` usa ativação idempotente por `pointerup`, `touchend` e `click`, evitando duplo toggle em UAs que emitem mais de um evento. `pointerdown` permanece apenas para bloquear scroll.
- Validação GPU pós-fix: Meadow/Neon inicial e pós-`Race Again`; pause abriu por tap, `Race Again → pause → Menu` passou, `pageErrors=[]`.
- Semântica HUD: pausa e resultado agora são `role=dialog`, `aria-modal=true`, títulos nomeados e ações com labels explícitos (`Restart race`, `Toggle sound`, `Open main menu`, `Race again`). Probe DOM Neon mobile `ok=true`.
- Revisão visual 4/4 dos overlays: legibilidade e hierarquia aceitáveis, sem overlap crítico. Recomendações restantes (localização, card mais opaco, confirmação de Restart, foco visual) são melhorias de produto sem evidência A/B suficiente para patch automático.

## Continuação autônoma — ciclo 5

- A inspeção de foco revelou que dialogs ARIA abriam sem mover o foco para uma ação útil.
- Correção em `HUD.js`: `showPause(true)` foca `Restart race`; `showFinish()` foca `Race again`, ambos com `preventScroll`.
- Probe GPU desktop confirmou: pause `activeElement=Restart race`, finish `activeElement=Race again`, `role=dialog`, `aria-modal=true`, `pageErrors=[]`.
